# Database Configuration
DB_HOST=localhost
DB_USER=family_album_user
DB_PASSWORD=root
DB_NAME=family_album
DB_PORT=3306

# JWTシークレットキー
# セキュリティのため、ランダムで十分に長い文字列を設定してください
JWT_SECRET_KEY=your_jwt_secret_key

# Application Environment (development/production)
ENVIRONMENT=development

# File Storage Configuration
#
# ⚠️ 重要: Docker環境での設定について
#
# この.envファイルはローカル開発用です。
# Docker環境では docker-compose.yml の environment セクションで設定してください。
#
# Docker環境でのルール:
# 1. パスは「コンテナ内のパス」を指定する（例: /app/storage/photos）
# 2. docker-compose.yml の volumes で「ホストパス:コンテナパス」をマウントする
# 3. 環境変数のパスとボリュームマウントのコンテナパスを一致させる
#
# ❌ 間違い: PHOTOS_STORAGE_PATH=/media/usbdrive/family_album/photos
#    → ホストパスを指定するとコンテナ内でマウントされず、データが永続化されない
#
# ✅ 正しい例:
#    環境変数: PHOTOS_STORAGE_PATH=/app/storage/photos
#    volumes:  /media/usbdrive/family_album/photos:/app/storage/photos
#
# 詳細は docs/production-deployment-guide.md を参照
#
# ローカル開発用（Dockerなし）:
PHOTOS_STORAGE_PATH=./storage/photos
THUMBNAILS_STORAGE_PATH=./storage/thumbnails

# File Upload Limits (in bytes)
MAX_UPLOAD_SIZE=20971520  # 20MB
ALLOWED_IMAGE_TYPES=image/jpeg,image/png,image/gif,image/webp,image/heic,image/heif

# Development vs Production Settings
# Development: Creates directories automatically, more verbose logging
# Production: Assumes directories exist, production-level logging
AUTO_CREATE_DIRS=true